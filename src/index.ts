// @ts-nocheck
/**
 * dsh-wzone — host half. The browser half (exports "./client") does all the
 * work; this node half is intentionally empty (no services, no routes).
 */
export const inject: string[] = []

export function apply(_ctx: unknown): void {}
